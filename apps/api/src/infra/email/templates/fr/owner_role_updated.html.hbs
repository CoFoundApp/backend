{{#> layout}}
  <p>Bonjour,</p>
  <p>Rôle mis à jour sur <strong>{{project_title}}</strong> :</p>
  <p><strong>{{member_name}}</strong> est maintenant <strong>{{role}}</strong>{{#if actor_name}} (par {{actor_name}}){{/if}}.</p>
  {{#if cta_url}}<p><a href="{{cta_url}}" class="btn">Voir les membres</a></p>{{/if}}
  <p>Merci,<br/>L'équipe {{app_name}}</p>
{{/layout}}
